build/
install/
log/
.venv/

# Perception model weights — large and not regenerable from source.
# best.pt (CPU/sim), best.hef (Hailo, real robot), NCNN exports live here.
models/
*.pyc
__pycache__/
.cache/
src/trac_ik/
.claude/
.vscode/
*.gv
*.pdf

# slam_toolbox serialized maps — large (tens of MB) and regenerable from a
# mapping run. The portable Nav2 map (greenhouse.pgm/.yaml) is committed instead.
*.posegraph
*.data
